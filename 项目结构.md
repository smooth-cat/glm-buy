# GLM Coding Sniper — 项目结构

## 概述

Python + Playwright 版本的 GLM Coding Plan Pro 自动抢购脚本。
每天 10:00 自动在 `open.bigmodel.cn` 抢购指定套餐。

原版：`glm-coding-sniper.user.js`（Tampermonkey 油猴脚本）

## 目录结构

```
glm-buy/
├── pyproject.toml                  # uv 项目配置（依赖、构建）
├── uv.lock                         # uv 依赖锁文件（自动生成）
├── 项目结构.md                      # 本文档
├── glm-coding-sniper.user.js       # 原始 JS 脚本（参考用，不运行）
├── logs/                           # 日志文件目录（自动创建）
│   └── glm-sniper-2026-05-17.log   # 按日轮转的日志文件
├── browser_profile/                # 浏览器持久化目录（保存登录态）
├── glm_sniper_pid.json             # productId 本地缓存（12 小时有效）
├── .venv/                          # uv 虚拟环境（自动创建）
└── src/
    └── glm_buy/
        ├── __init__.py             # 包标识
        ├── main.py                 # ★ 程序入口
        ├── config.py               # 配置（套餐、时间、重试参数）
        ├── logger.py               # 日志系统（loguru）
        ├── notifier.py             # 系统通知 + 音频提醒
        ├── api.py                  # 直接 HTTP API 调用（httpx）
        ├── product.py              # 商品 ID 管理（识别 + 缓存）
        ├── mouse.py                # 人类化鼠标移动 + 点击
        ├── dom_reader.py           # DOM 只读检查（找按钮/弹窗）
        ├── browser.py              # 浏览器管理 + 请求拦截
        └── scheduler.py            # ★ 核心调度器（倒计时 + 抢购循环）
```

## 模块职责

| 模块 | 行数 | 职责 | 对应 JS 版 |
|---|---|---|---|
| `main.py` | ~54 | 入口，构建配置，启动调度器 | `init()` |
| `config.py` | ~54 | 配置 dataclass + 常量映射表 | `CONFIG` 对象 |
| `logger.py` | ~34 | loguru 初始化（终端 + 文件） | `log()` / `setStatus()` |
| `notifier.py` | ~48 | macOS 系统通知 + 音频 | `notify()` / `playAlert()` |
| `api.py` | ~118 | 直接 HTTP API 调用 | `fetchProductIdDirectly()` |
| `product.py` | ~326 | 商品 ID 识别、缓存、匹配 | `identifyPlanFromProduct()` 等 |
| `mouse.py` | ~111 | 贝塞尔曲线鼠标移动 + 点击 | `tryClickPurchaseButton()` 点击部分 |
| `dom_reader.py` | ~347 | DOM 只读：找按钮、检测弹窗 | `tryClickXxxButton()` 查找部分 |
| `browser.py` | ~319 | 浏览器启动 + `page.route()` 拦截 | `JSON.parse` / `fetch` / `XHR` 劫持 |
| `scheduler.py` | ~713 | 核心调度：倒计时 → 抢购循环 → 恢复 | `startCountdown()` / `startSnipe()` 等 |

## 数据流

```
用户运行 main.py
  │
  ├─→ logger.setup_logging()           # 初始化日志
  ├─→ Scheduler(config)
  │     ├─→ ProductManager()           # 商品管理器
  │     ├─→ APIClient()                # 直接 API 客户端
  │     └─→ Scheduler.run()
  │           │
  │           ├─→ BrowserManager.start()
  │           │     ├─→ 启动 Chromium（持久化登录态）
  │           │     ├─→ page.route() 拦截 API 响应
  │           │     └─→ 导航到购买页面
  │           │
  │           ├─→ DOMReader(page)       # 包装 Playwright page
  │           ├─→ Mouse(page)           # 包装 Playwright page
  │           │
  │           ├─→ 加载本地缓存 productId
  │           ├─→ 启动时间校准（异步）
  │           ├─→ 启动定期 productId 检查（异步）
  │           │
  │           └─→ 主循环
  │                 │
  │                 ├─→ [倒计时] _run_countdown()
  │                 │     每 0.5s 更新终端显示
  │                 │     T-60s: 预热 productId
  │                 │     T-10s: 自动刷新页面
  │                 │     T-200ms: ─────────┐
  │                 │                        │
  │                 ├─→ [抢购] _start_snipe() ◄┘
  │                 │     1. _select_billing_period() → mouse.click()
  │                 │     2. 等待/获取 productId
  │                 │     3. 主循环（最多 300 次）:
  │                 │        a. DOMReader.find_purchase_button() → 坐标
  │                 │        b. Mouse.move_to() + Mouse.click() → 点击
  │                 │        c. DOMReader.detect_modal() → 弹窗判断
  │                 │        d. DOMReader.detect_qr_code() → 成功检测
  │                 │
  │                 ├─→ [恢复] auto_recovery_check()
  │                 │     页面错误 → 刷新
  │                 │
  │                 ├─→ [重触发] auto_snipe_on_ready()
  │                 │     页面恢复且有按钮 → 重新抢购
  │                 │
  │                 ├─→ [探测] probe_sold_out_status()
  │                 │     10:02 后 API 直查真实售罄
  │                 │
  │                 └─→ 等待到明天 → 重置状态 → 循环
```

## 运行方式

```bash
cd glm-buy

# 首次运行需要安装依赖
uv sync
uv run playwright install chromium

# 运行脚本
uv run python -m glm_buy.main
```

## 配置修改

编辑 `src/glm_buy/main.py` 中的 `build_config()` 函数：

```python
def build_config() -> Config:
    return Config(
        plan_priority=[
            PlanPriority(plan="lite", billing_period="quarterly"),  # 首选
            PlanPriority(plan="pro", billing_period="quarterly"),   # 候补
        ],
        target_hour=10,        # 目标小时
        target_minute=0,       # 目标分钟（每天 10:00 开抢）
        advance_ms=200,        # 提前 200ms 开始点击
        max_retries=300,       # 最多 300 次重试（30 秒）
        headless=False,        # 必须 False（鼠标模拟需要可见窗口）
    )
```

## JS → Python 关键技术映射

| JS 原版 | Python 替代方案 | 原因 |
|---|---|---|
| `JSON.parse` 劫持 | `page.route()` 响应修改 | Playwright 网络层拦截 |
| `window.fetch` 劫持 | `page.route()` 请求修改 | 同上 |
| `XMLHttpRequest` 劫持 | `page.route()` 统一拦截 | 同上 |
| `element.click()` | `mouse.move()` + `mouse.click()` | 用户要求模拟真实鼠标 |
| `MutationObserver` | `page.locator()` 轮询 + `evaluate()` | Playwright 原生选择器 |
| Vue 组件遍历 (`$children`) | 不实现 | `page.route()` 替代 |
| `localStorage` | JSON 文件 | Python 无浏览器 Storage API |
| `Notification` API | `osascript` 系统通知 | macOS 系统 API |
| `AudioContext` | `afplay` 系统音频 | macOS 系统命令 |
| UI 浮窗 (`createOverlay`) | 终端 `print` 倒计时 | 用户要求无 UI |
| `GM_info` | 不适用 | 非浏览器环境 |
