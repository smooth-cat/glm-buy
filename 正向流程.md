# GLM Coding Sniper — 正向流程（Happy Path）

从启动到抢购成功，按时间顺序列出每一个被调用的函数及其作用。

---

## 阶段 0：启动

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 0.1 | `main.py:74` | `asyncio.run(main())` | 创建事件循环，运行 `main()` 协程 |

---

## 阶段 1：初始化

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 1.1 | `main.py:50` | `logger.setup_logging()` | 初始化 loguru：终端彩色输出 + 按日轮转日志文件 |
| 1.2 | `main.py:53` | `build_config()` | 构建 `Config` 对象（套餐优先级、抢购时间、重试参数等） |
| 1.3 | `main.py:54` | `Scheduler(config)` | 创建调度器实例，初始化 ProductManager 和 APIClient |
| 1.4 | `main.py:58` | `scheduler.run()` | 主入口 → 进入阶段 2 |

---

## 阶段 2：启动浏览器

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 2.1 | `scheduler.py:745` | `BrowserManager(...)` | 创建浏览器管理器实例 |
| 2.2 | `scheduler.py:753` | `browser.start()` | 启动 Playwright + Chromium 持久化上下文 |
| 2.2.1 | `browser.py:135` | `async_playwright().start()` | 启动 Playwright 引擎 |
| 2.2.2 | `browser.py:146` | `chromium.launch_persistent_context(...)` | 启动 Chromium 浏览器（持久化登录态到 `browser_profile/`） |
| 2.2.3 | `browser.py:175` | `context.new_page()` | 创建新标签页 |
| 2.2.4 | `browser.py:176` | `_setup_routes()` | 注册 `page.route()` 拦截所有 `/api/biz/pay/` 请求 |
| 2.2.5 | `browser.py:239` | `page.route(regex, _handle_api_route)` | 绑定拦截处理函数（请求指纹随机化 + productId 注入 + soldOut 改 false） |
| 2.2.6 | `browser.py:177` | `_setup_auth_capture()` | 注册 `page.on("request")` 监听器，从页面请求中捕获 Authorization 头 |
| 2.2.7 | `browser.py:181` | `page.goto(purchase_url)` | 导航到购买页面 `https://open.bigmodel.cn/glm-coding` |
| 2.2.8 | `browser.py:186` | `page.goto(...)` (条件) | 如果被重定向到限流页面，重新跳回购买页 |

---

## 阶段 3：初始化辅助模块

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 3.1 | `scheduler.py:756` | `DOMReader(page)` | 创建 DOM 只读检查器（找按钮、检测弹窗） |
| 3.2 | `scheduler.py:757` | `Mouse(page)` | 创建鼠标模拟器（贝塞尔曲线移动 + 点击） |
| 3.3 | `scheduler.py:760` | `asyncio.create_task(_delayed_time_calibration())` | 后台任务：延迟 2 秒后校准服务器时间 |
| 3.3.1 | `scheduler.py:837` | `api_client.calibrate_time()` | HEAD 请求获取服务器 Date 头，计算时间偏差 |
| 3.4 | `scheduler.py:763` | `product_mgr.get_product_id(plan, period)` | 从本地 JSON 缓存加载 productId（12 小时 TTL） |
| 3.5 | `scheduler.py:771` | `asyncio.create_task(_periodic_pid_check())` | 后台任务：每 3 秒检查一次 productId 是否就绪，每 6 秒主动调 API |

---

## 阶段 4：倒计时

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 4.1 | `scheduler.py:800` | `_run_countdown()` | 倒计时循环，每 0.5 秒更新终端显示 |

### 倒计时里程碑：

| 时间点 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| **T-60s** | `scheduler.py:320` | `_fetch_product_id_directly()` | 预热：绕过浏览器直接调 API 获取 productId |
| | `scheduler.py:566` | `browser.auth_header` | 获取已捕获的 Authorization 头 |
| | `scheduler.py:576` | `api_client.batch_preview()` | POST `/api/biz/pay/batch-preview` 获取商品列表 |
| | `scheduler.py:578` | `product_mgr.capture_product_id_from_data(...)` | 解析 API 响应，通过价格识别套餐、提取 productId |
| | `product.py:93` | `identify_plan_from_product(item)` | 价格 49→lite, 149→pro, 469→max；campaignName→计费周期 |
| | `scheduler.py:591` | `api_client.product_info()` | 兜底：GET `/api/biz/pay/productinfo` |
| **T-10s** | `scheduler.py:314` | `browser.reload()` | 刷新页面以获取最新状态 |
| **T-3s** | `scheduler.py:325` | (仅日志) | TCP 预热标记（浏览器已维护连接池） |
| **T-200ms** | `scheduler.py:331` | `_start_snipe()` | 提前 200ms 进入抢购 → 阶段 5 |

---

## 阶段 5：抢购循环

### 5.1 准备阶段

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 5.1.1 | `scheduler.py:364` | `_select_billing_period()` | 鼠标点击套餐的计费周期标签（如"包季"） |
| | `dom_reader.py:226` | `dom.find_billing_period_tab(plan, period)` | 找到套餐卡片内的周期标签 → 返回坐标 |
| | `mouse.py:106` | `mouse.click(x, y)` | 贝塞尔曲线移动 + 真实鼠标点击 |
| | `mouse.py:117` | `mouse.move_to(x, y)` | 从当前位置沿贝塞尔曲线移动到目标 |
| | `mouse.py:18` | `_bezier_curve(start, end, steps)` | 生成带弧度的路径点（模拟人类手臂运动） |
| | `mouse.py:125` | `page.mouse.click(x, y)` | Playwright 底层鼠标点击 |
| 5.1.2 | `scheduler.py:367` | `product_mgr.get_product_id(plan, period)` | 检查 productId 是否已从 API 响应中捕获 |
| 5.1.3 | `scheduler.py:374` | `_wait_for_product_id(3000)` | 若未就绪，每 100ms 轮询一次，最多等 3 秒 |
| 5.1.4 | `scheduler.py:376` | `_fetch_product_id_directly()` | 若仍未就绪，主动调 API 获取 |
| 5.1.5 | `scheduler.py:379` | `browser.product_id = pid` | 将 productId 同步到浏览器（供 `page.route()` 注入用） |

### 5.2 点击循环（每 100ms 一轮，最多 300 轮 = 30 秒）

每轮执行以下步骤：

| 步骤 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| **A** | `scheduler.py:422` | `dom.find_purchase_button(plan)` | 在套餐卡片内查找包含"购买"等关键词的按钮 → 返回坐标 |
| | `dom_reader.py:103` | `find_purchase_button()` | 策略1：文本匹配（先找卡片→再找按钮）；策略2：data-plan 选择器 |
| | `dom_reader.py:140` | `_has_nearby_plan_text(btn, keys)` | 向上遍历 5 层父元素，确认按钮附近有套餐关键词 |
| | `scheduler.py:424` | `mouse.click(x, y)` | 贝塞尔曲线移动到按钮位置并点击 |
| **B** | `scheduler.py:428` | `dom.find_confirm_button()` | 在可见弹窗内查找"确认"/"立即支付"等按钮 → 返回坐标 |
| | `scheduler.py:430` | `mouse.click(x, y)` | 贝塞尔曲线移动并点击确认按钮 |
| **C** | `scheduler.py:433` | `dom.detect_modal()` | 检测页面上是否有验证码或支付弹窗 |
| | `dom_reader.py:293` | `detect_modal()` | 遍历 [class*="modal"] 元素，检查文本含"验证"还是"支付" → 返回类型 |
| | | | 若无弹窗 → 继续循环；若验证码弹窗 → 暂停+通知用户；若支付弹窗 → 阶段6 |
| **D** | `scheduler.py:467` | `dom.detect_qr_code()` | 检测页面是否出现支付二维码（canvas / img[src*="qr"]） |

---

## 阶段 6：抢购成功

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 6.1 | `dom_reader.py:352` | `detect_qr_code()` | 返回 `True`（检测到 canvas 或支付二维码图片） |
| 6.2 | `scheduler.py:469` | `_order_created = True` | 标记订单已创建，停止点击循环 |
| 6.3 | `scheduler.py:471` | `browser.order_created = True` | 同步到浏览器（停止请求拦截中的 productId 注入） |
| 6.4 | `scheduler.py:472` | `async_play_alert()` | 播放 3 次系统提示音 |
| | `notifier.py:64` | `play_alert()` | `afplay /System/Library/Sounds/Glass.aiff` × 3 |
| 6.5 | `scheduler.py:473` | `async_notify(title, body)` | 发送 macOS 系统通知 |
| | `notifier.py:56` | `notify(title, body)` | `osascript display notification "GLM 抢购成功！支付二维码已出现，快去扫码！"` |
| 6.6 | `scheduler.py:477` | `break` | 退出抢购循环 |

---

## 阶段 7：收尾

| 顺序 | 文件:行 | 函数 | 作用 |
|---|---|---|---|
| 7.1 | `scheduler.py:811` | `probe_sold_out_status()` | 立即返回（`_order_created=True`，无需探测） |
| 7.2 | `scheduler.py:814` | 状态重置 | 重置所有标志位（`_confirmed_sold_out=False` 等），等待明天 |
| 7.3 | `main.py:67` | `browser.stop()` | 关闭浏览器上下文和 Playwright 引擎 |

---

## 关键路径速览

```
main()
  └─ scheduler.run()
       ├─ browser.start()          # 启动 Chromium + 设置 page.route() 拦截
       ├─ DOMReader(page)          # DOM 只读检查器
       ├─ Mouse(page)              # 鼠标模拟器
       ├─ _run_countdown()         # 倒计时（每 0.5s 更新终端）
       │    ├─ T-60s: _fetch_product_id_directly() → api.batch_preview()
       │    ├─ T-10s: browser.reload()
       │    └─ T-200ms: _start_snipe()
       │              ├─ _select_billing_period() → dom.find_billing_period_tab() → mouse.click()
       │              └─ [循环 ×300]
       │                   ├─ dom.find_purchase_button() → mouse.click()
       │                   ├─ dom.find_confirm_button() → mouse.click()
       │                   ├─ dom.detect_modal()
       │                   └─ dom.detect_qr_code()  ← ★ 成功
       │                         ├─ async_play_alert()    # 播放提示音
       │                         └─ async_notify()         # 系统弹窗通知
       └─ browser.stop()          # 关闭浏览器
```

## 涉及的 10 个源文件

| 文件 | 核心职责 |
|---|---|
| `main.py` | 入口，构建配置，启动调度器 |
| `config.py` | 配置 dataclass + 常量映射表 |
| `logger.py` | loguru 初始化 |
| `browser.py` | 浏览器启动 + `page.route()` 请求拦截 |
| `dom_reader.py` | DOM 只读（找按钮、检测弹窗、检测二维码） |
| `mouse.py` | 贝塞尔曲线鼠标移动 + 点击 |
| `product.py` | 商品 ID 识别、捕获、缓存、匹配 |
| `api.py` | 直接 HTTP API 调用（batch-preview、productinfo、时间校准） |
| `scheduler.py` | 核心调度（倒计时 → 抢购循环 → 恢复） |
| `notifier.py` | macOS 系统通知 + 音频提示 |
